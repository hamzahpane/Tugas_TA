import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  contair: {
    backgroundColor: 'red ',
    shadowColor: '#b6c1cd',
    shadowOffset: {
      width: 10,
      height: 10,
    },
    shadowOpacity: 0.25,
    shadowRadius: 6,
    elevation: 10,
  },
});
export default styles;
